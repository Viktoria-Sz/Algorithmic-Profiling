---
title: "JuSaW data"
author: "Viktoria Szabo"
date: "13 3 2022"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

## R Markdown

This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:

```{r cars}
summary(cars)
```

## Including Plots

You can also embed plots, for example:

```{r pressure, echo=FALSE}
plot(pressure)
```

Note that the `echo = FALSE` parameter was added to the code chunk to prevent printing of the R code that generated the plot.

## Data Descriptives

# Basic descriptives like oberservation numbers
# Distribution dependent variable
# Distribution of some interesting variables?
# Heatmap AMS variables with dependent
# -> Anteil an Personen mit der Variablenausprägung, die es geschafft haben
# Heatmap other variables with dependent

## Modeling

# Test-Train set-up
# Original logistic regression AMS youth model coefficients
# Original logistic regression AMS youth model new fit
# Logistic regression with other variables
# Random forest with AMS youth
# Random forest with other variables

## Evaluation

# Heatmap with performance measures
# Heatmap with fairness measures

